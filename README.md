# CircleCI deployment image with AWSCLI and NODE included

Docker image used for pushing to AWS ECR from CircleCI.

It includes the following packages:

- Docker
- Git
- AWS-cli
- Node
- librarieris to build node-gyp
